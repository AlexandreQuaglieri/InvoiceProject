import { QuotesContent } from '@/components/quotes/quotes-content'

// RSC minimal : la donnée vient du store live seedé par le layout (dashboard).
export default function QuotesPage() {
  return <QuotesContent />
}
